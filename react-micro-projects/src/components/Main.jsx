import React from 'react'

const Main = () => {
  // const num = 2;
  // const header = "Give it your best shot";
  // const multiply = (a, b) => a * b;
  // const numArr = [1,2,3,4,5];

  
  return (
    <main>
      

        {/* <h2>Main Content</h2>
        <p className='ok'>{header}</p>
        <form>
          <label htmlFor='name'>Name</label>
          <input type='text' id="name" placeholder='Hi' />
        </form>
        <p>{num * num * num}</p>
        <p>My Friends List: {['Alex', 'John', 'Wkwkwkwk']}</p>
        <p>2 * 10 = {multiply(2, 10)}</p>
        <ul id={Math.random()}>
        {numArr.map(
          (number) => (
              <li>{number}</li>  
          )
        )}
        </ul> */}
    </main>
  )
}

export default Main